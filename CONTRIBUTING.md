# Contributing to Maas

Create a focused branch, include database migrations for schema changes, and run typecheck, lint and build before opening a pull request. Any change to tenant data access must include an explicit row-level-security review.
