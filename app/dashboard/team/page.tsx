import { getActiveCompany } from "@/lib/auth"; import { Card } from "@/components/ui/card";
export default async function Team(){const {supabase,company}=await getActiveCompany();const {data}=company?await supabase.from("company_members").select("user_id,role,created_at").eq("company_id",company.id):{data:[]};return <><h1 className="text-3xl font-bold">Team and permissions</h1><p className="mt-2 text-slate-500">Owner, admin, analyst and viewer roles are enforced at database level.</p><Card className="mt-7 overflow-hidden"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-900"><tr><th className="px-5 py-3">User ID</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Added</th></tr></thead><tbody className="divide-y dark:divide-slate-800">{data?.map(m=><tr key={m.user_id}><td className="px-5 py-4 font-mono text-xs">{m.user_id}</td><td className="px-5 py-4 font-medium capitalize">{m.role}</td><td className="px-5 py-4 text-slate-500">{new Date(m.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></Card></>}
