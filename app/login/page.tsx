import Link from "next/link"; import { AuthForm } from "@/components/auth-form"; import { Logo } from "@/components/logo"; import { Card } from "@/components/ui/card"; import { DataIllustration } from "@/components/data-illustration";
export default async function Login({searchParams}:{searchParams:Promise<{next?:string}>}){const nextPath=(await searchParams).next;return <main className="grid min-h-screen items-center gap-8 px-6 py-10 lg:grid-cols-[minmax(0,440px)_minmax(0,560px)] lg:justify-center"><Card className="w-full p-7"><Logo/><h1 className="mt-8 text-2xl font-bold text-slate-950 dark:text-white">Welcome back</h1><p className="mb-6 mt-2 text-sm text-slate-500 dark:text-slate-400">Sign in to your Maas workspace.</p><AuthForm mode="login" nextPath={nextPath}/><p className="mt-5 text-center text-sm text-slate-500">New to Maas? <Link className="font-semibold text-teal-700 dark:text-teal-300" href="/signup">Create an account</Link></p></Card><div className="hidden lg:block"><DataIllustration compact/></div></main>}
