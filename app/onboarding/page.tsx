import { createCompany } from "./actions"; import { requireUser } from "@/lib/auth"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Button } from "@/components/ui/button"; import { Logo } from "@/components/logo";
export default async function Onboarding(){await requireUser();return <main className="grid min-h-screen place-items-center px-6"><Card className="w-full max-w-xl p-8"><Logo/><div className="mt-8"><span className="text-sm font-semibold text-indigo-600">Workspace setup</span><h1 className="mt-2 text-3xl font-bold">Create your first company</h1><p className="mt-2 text-slate-500">Data is isolated by company and protected with row-level security.</p></div><form action={createCompany} className="mt-7 space-y-5"><label className="block text-sm font-medium">Company name<Input name="name" required className="mt-2" placeholder="Acme Corporation"/></label><label className="block text-sm font-medium">Industry<Input name="industry" className="mt-2" placeholder="Retail, logistics, manufacturing…"/></label><label className="block text-sm font-medium">Timezone<select name="timezone" defaultValue="Africa/Mogadishu" className="mt-2 h-11 w-full rounded-xl border bg-transparent px-3"><option>Africa/Mogadishu</option><option>UTC</option><option>Europe/London</option><option>America/New_York</option><option>Asia/Dubai</option></select></label><Button className="w-full">Create workspace</Button></form></Card></main>}
